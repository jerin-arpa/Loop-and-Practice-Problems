// Declare an array for all the topics that you have learned last few days. Display all the elements of the array by using a while loop 

var topics = ['html', 'html5', 'css', 'css5', 'tailwind', 'daisyui', 'array', 'if-else', 'for-loop', 'while-loop'];
var num = 0;

while (num < topics.length) {
    console.log(topics[num]);
    num++;
}